export interface IDialogData {
    mode: string;
    itemData: any
}

export interface FilmDialogData {
    mode: string;
    filmData: any;
  }

  export interface BookDialogData {
    mode: string;
    filmData: any;
  }